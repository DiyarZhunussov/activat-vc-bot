# Activat VC Telegram Bot

Production-ready бот для супергруппы венчурного капитала, оптимизированный для **Render.com** + **Supabase**.

## 🚀 Возможности

### Социальные функции
- `/shoutout @user причина` - благодарность участникам (только админы)
- `/challenge текст` - еженедельные челленджи с автоматическими итогами
- `/network текст` - публикация запросов на нетворкинг
- `/mentor тема` - автоматический подбор менторов

### Питчи и стартапы
- `/ratepitch` - голосование 1-5⭐ с результатами через 24ч
- Авто-архивация сообщений с `#pitch`
- Ежемесячный топ-3 питчей по лайкам

### Аналитика
- `/growth` - статистика роста, retention, активности (только админы)
- Анализ настроений по эмодзи (еженедельно)
- Retention отчеты 7/30 дней

### Технические
- `/search слово` - поиск в последних 100 сообщениях
- `/restart` - информация о перезапуске
- Uptime мониторинг каждые 5 минут
- Полное логирование в Supabase

## 📋 Требования

- Python 3.10+
- Telegram Bot Token
- Supabase проект (бесплатный tier)
- Render.com аккаунт (бесплатный tier)

## 🔧 Установка

### Шаг 1: Создание Telegram бота

1. Откройте [@BotFather](https://t.me/botfather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Сохраните токен
5. Добавьте бота в супергруппу как администратора

### Шаг 2: Получение ID группы и топиков

```python
# Запустите этот код локально для получения IDs:
import logging
from telegram.ext import Application, MessageHandler, filters

TOKEN = "ваш_токен"

logging.basicConfig(level=logging.INFO)

async def print_ids(update, context):
    print(f"Chat ID: {update.effective_chat.id}")
    if update.message.message_thread_id:
        print(f"Thread ID: {update.message.message_thread_id}")

app = Application.builder().token(TOKEN).build()
app.add_handler(MessageHandler(filters.ALL, print_ids))
app.run_polling()
```

Отправьте сообщения в группу и топики - увидите ID в консоли.

### Шаг 3: Настройка Supabase

1. Зарегистрируйтесь на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Дождитесь инициализации (2-3 минуты)
4. Откройте **SQL Editor**
5. Вставьте и выполните код из `supabase_schema.sql`
6. Перейдите в **Settings → API**
7. Скопируйте:
   - Project URL
   - `anon` key (для тестирования) или `service_role` key (для production)

### Шаг 4: Деплой на Render.com

#### Вариант A: Через GitHub (рекомендуется)

1. Создайте репозиторий на GitHub
2. Запушьте код:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/activat-vc-bot.git
git push -u origin main
```

3. Зайдите на [render.com](https://render.com)
4. Нажмите **New +** → **Web Service**
5. Подключите GitHub репозиторий
6. Настройте:
   - **Name**: `activat-vc-bot`
   - **Region**: Frankfurt (ближайший к вам)
   - **Branch**: `main`
   - **Runtime**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`
   - **Plan**: Free

7. Добавьте Environment Variables (см. ниже)
8. Нажмите **Create Web Service**

#### Вариант B: Через Render CLI

```bash
# Установите Render CLI
npm install -g @render/cli

# Логин
render login

# Деплой
render up
```

### Шаг 5: Настройка Environment Variables на Render

В разделе **Environment** добавьте:

```
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
TELEGRAM_CHAT_ID=-1003812789640
TELEGRAM_ADMIN_IDS=123456789,987654321,555555555
TELEGRAM_DISCUSSION_THREAD_ID=5
TELEGRAM_SIX_HANDSHAKES_THREAD_ID=6
TELEGRAM_FLOOD_THREAD_ID=8
TELEGRAM_NETWORK_THREAD_ID=7
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=ваш_ключ_supabase
```

**Важно**: Используйте `service_role` key для production, чтобы обойти RLS.

## ✅ Проверка работы

После деплоя проверьте:

1. **Логи Render**:
   - Откройте Dashboard → Logs
   - Должно быть: `✅ Supabase подключен` и `✅ Bot initialized successfully`

2. **Бот онлайн**:
   ```
   Отправьте /start боту в личку
   Должен ответить приветствием
   ```

3. **Команды работают**:
   ```
   В группе: /help
   Админ: /growth
   ```

4. **Проверка Supabase**:
   ```sql
   -- В SQL Editor:
   SELECT * FROM bot_logs ORDER BY timestamp DESC LIMIT 5;
   SELECT * FROM users ORDER BY join_date DESC LIMIT 5;
   ```

## 🔄 Обновление бота

### Автоматическое (через Git)

```bash
# Внесите изменения в код
git add .
git commit -m "Update features"
git push origin main
```

Render автоматически задеплоит новую версию!

### Ручное (через Dashboard)

1. Откройте Render Dashboard
2. Выберите ваш сервис
3. Нажмите **Manual Deploy** → **Deploy latest commit**

## 📊 Мониторинг

### Логи Render

Dashboard → Logs показывает:
- Запуск бота
- Обработку команд
- Ошибки
- Uptime проверки

### Логи Supabase

```sql
-- Ошибки бота
SELECT * FROM bot_logs WHERE level = 'error' ORDER BY timestamp DESC;

-- Активность пользователей
SELECT username, COUNT(*) as messages 
FROM group_logs 
WHERE timestamp > NOW() - INTERVAL '7 days'
GROUP BY username 
ORDER BY messages DESC;

-- Топ питчей
SELECT username, likes, LEFT(text, 100) as preview
FROM pitches 
ORDER BY likes DESC 
LIMIT 10;
```

## 🐛 Troubleshooting

### Бот не запускается

1. **Проверьте переменные окружения**:
   - Все ли установлены?
   - Нет ли опечаток?

2. **Проверьте токен**:
```bash
curl https://api.telegram.org/bot<YOUR_TOKEN>/getMe
```

3. **Проверьте логи Render**:
   - Ищите ошибки с ❌

### Бот не отвечает на команды

1. **Права бота**:
   - Бот должен быть администратором группы
   - Права на чтение/отправку сообщений

2. **Проверьте Supabase**:
   - Выполнен ли SQL скрипт?
   - Правильный ли ключ?

### База данных не работает

1. **RLS проблемы**:
```sql
-- Отключите RLS для тестирования:
ALTER TABLE users DISABLE ROW LEVEL SECURITY;
ALTER TABLE group_logs DISABLE ROW LEVEL SECURITY;
-- И т.д. для всех таблиц
```

2. **Или используйте service_role key** вместо anon key

### Render засыпает

Бесплатный tier Render засыпает после 15 минут неактивности:
- Бот использует **polling**, поэтому остается активным
- Uptime проверки каждые 5 минут держат бота живым

Если нужна 100% uptime → upgrade на Starter plan ($7/мес)

## 💰 Стоимость

### Бесплатный tier:
- ✅ Render: Free Web Service
- ✅ Supabase: Free tier (500MB storage, 50K requests/день)
- 💡 Достаточно для сообщества до 500 человек

### При росте:
- Render Starter: $7/мес (постоянный uptime)
- Supabase Pro: $25/мес (8GB storage, 5M requests/день)

## 🔒 Безопасность

1. **Не коммитьте .env** - используйте Render Environment Variables
2. **Service role key** - храните безопасно, только на Render
3. **Регулярно меняйте токены**
4. **Backup Supabase** - включите в Settings

## 📈 Оптимизация

### Для больших сообществ:

1. **Индексы Supabase**:
```sql
-- Добавьте индексы для часто используемых запросов
CREATE INDEX idx_messages_search ON group_logs USING gin(to_tsvector('russian', text));
```

2. **Connection Pooling**:
   - Включите в Supabase Settings → Database

3. **Кэширование**:
   - Используйте Redis (Render Add-on) для кэша

## 🆘 Поддержка

- **Проблемы**: создайте Issue в GitHub
- **Вопросы**: напишите в топик "Обсуждения"
- **Документация**: [Render Docs](https://render.com/docs), [Supabase Docs](https://supabase.com/docs)

## 📝 Roadmap

- [ ] Webhook режим (вместо polling)
- [ ] Расширенная аналитика с графиками
- [ ] Система достижений
- [ ] AI-powered рекомендации
- [ ] Интеграция с календарем

---

Made with ❤️ for Activat VC Community
Optimized for Render.com + Supabase
